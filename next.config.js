/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
// /** @type {import('next').NextConfig} */
// const path = require("path");
// const nextConfig = {
//   reactStrictMode: true,
//   images: {
//     domains: ["upload.wikimedia.org", "s.net.vn"],
//   },

//   webpack: (config) => {
//     config.resolve.alias["@styles"] = path.join(__dirname, "/styles");
//     return config;
//   },
// };

// module.exports = nextConfig;
